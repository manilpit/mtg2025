<!DOCTYPE html>
<html lang="no">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Norgesmesterskapet i Magic: The Gathering 2025</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background-color: #0a0e1a;
            color: #e0e6ed;
            line-height: 1.6;
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .header {
            padding: 60px 0 80px;
            text-align: center;
            background: linear-gradient(135deg, #0a0e1a 0%, #1a1f2e 50%, #2c3e50 100%);
            position: relative;
        }

        .header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grid" width="10" height="10" patternUnits="userSpaceOnUse"><path d="M 10 0 L 0 0 0 10" fill="none" stroke="rgba(255,255,255,0.05)" stroke-width="0.5"/></pattern></defs><rect width="100" height="100" fill="url(%23grid)"/></svg>');
            opacity: 0.3;
        }

        .logo {
            font-size: 2.8em;
            font-weight: 700;
            color: #ffffff;
            margin-bottom: 12px;
            letter-spacing: -1px;
            position: relative;
            z-index: 1;
        }

        .subtitle {
            font-size: 1.2em;
            color: #8892b0;
            font-weight: 400;
            margin-bottom: 20px;
            position: relative;
            z-index: 1;
        }

        .year {
            font-size: 4em;
            font-weight: 100;
            color: #64b5f6;
            letter-spacing: 8px;
            position: relative;
            z-index: 1;
            text-shadow: 0 0 30px rgba(100, 181, 246, 0.3);
        }

        .nav-section {
            background-color: #1e2a3a;
            border-bottom: 1px solid #34495e;
            padding: 0;
            margin-bottom: 60px;
        }

        .nav-container {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 60px;
        }

        .nav-title {
            color: #64b5f6;
            font-size: 1.1em;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .main-content {
            padding: 0 0 80px;
        }

        .hero-card {
            background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%);
            border: 1px solid #4a5568;
            border-radius: 12px;
            padding: 40px;
            margin-bottom: 60px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        .hero-title {
            font-size: 1.8em;
            font-weight: 600;
            color: #ffffff;
            margin-bottom: 16px;
        }

        .hero-description {
            font-size: 1.1em;
            color: #a0aec0;
            max-width: 600px;
            margin: 0 auto;
        }

        .events-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 24px;
            margin-top: 40px;
        }

        .event-card {
            background: #1a202c;
            border: 1px solid #2d3748;
            border-radius: 8px;
            padding: 28px;
            text-decoration: none;
            color: inherit;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .event-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(90deg, #64b5f6, #42a5f5);
            transform: scaleX(0);
            transition: transform 0.3s ease;
        }

        .event-card:hover::before {
            transform: scaleX(1);
        }

        .event-card:hover {
            border-color: #4a5568;
            transform: translateY(-4px);
            box-shadow: 0 12px 40px rgba(0, 0, 0, 0.4);
        }

        .event-type {
            display: inline-block;
            font-size: 0.8em;
            color: #64b5f6;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 12px;
            font-weight: 600;
        }

        .event-title {
            font-size: 1.3em;
            font-weight: 600;
            color: #ffffff;
            margin-bottom: 12px;
            line-height: 1.3;
        }

        .event-description {
            color: #a0aec0;
            font-size: 0.95em;
            line-height: 1.5;
        }

        .main-event .event-type {
            color: #f56565;
        }

        .main-event::before {
            background: linear-gradient(90deg, #f56565, #e53e3e);
        }

        .side-event .event-type {
            color: #68d391;
        }

        .side-event::before {
            background: linear-gradient(90deg, #68d391, #48bb78);
        }

        .qualifier .event-type {
            color: #fbb6ce;
        }

        .qualifier::before {
            background: linear-gradient(90deg, #fbb6ce, #ed64a6);
        }

        .footer {
            background-color: #1a202c;
            border-top: 1px solid #2d3748;
            padding: 40px 0;
            text-align: center;
            margin-top: 60px;
        }

        .footer-content {
            color: #718096;
            font-size: 0.9em;
        }

        .footer-brand {
            color: #64b5f6;
            font-weight: 600;
            margin-bottom: 8px;
        }

        @media (max-width: 768px) {
            .logo {
                font-size: 2.2em;
            }
            
            .year {
                font-size: 2.8em;
                letter-spacing: 4px;
            }
            
            .hero-card {
                padding: 28px 20px;
            }
            
            .hero-title {
                font-size: 1.5em;
            }
            
            .events-grid {
                grid-template-columns: 1fr;
                gap: 20px;
            }
            
            .event-card {
                padding: 24px 20px;
            }
        }

        @media (max-width: 480px) {
            .container {
                padding: 0 15px;
            }
            
            .hero-card {
                padding: 24px 16px;
            }
        }
    </style>
</head>
<body>
    <header class="header">
        <div class="container">
            <h1 class="logo">Norgesmesterskapet</h1>
            <p class="subtitle">Magic: The Gathering</p>
            <div class="year">2025</div>
        </div>
    </header>

    <div class="nav-section">
        <div class="container">
            <div class="nav-container">
                <div class="nav-title">Events & Billetter</div>
            </div>
        </div>
    </div>

    <main class="main-content">
        <div class="container">
            <div class="hero-card">
                <h2 class="hero-title">Norges største Magic: The Gathering-turnering</h2>
                <p class="hero-description">
                    Bli med på årets mest prestisjetunge Magic-turnering. Her samles spillere fra hele Norge 
                    for å kjempe om tittelen som landets beste Magic-spiller. Finn informasjon og kjøp billetter nedenfor.
                </p>
            </div>

            <div class="events-grid">
                <a href="#" class="event-card" onclick="alert('Lenke til infografikk - implementer riktig URL')">
                    <div class="event-type">Informasjon</div>
                    <div class="event-title">Infografikk NM 2025</div>
                    <div class="event-description">
                        Komplett oversikt over turneringsoppsett, tidsplan og alle viktige detaljer for deltakere
                    </div>
                </a>

                <a href="#" class="event-card main-event" onclick="alert('Lenke til Standard Main Event - implementer riktig URL')">
                    <div class="event-type">Hovedturnering</div>
                    <div class="event-title">Main Event - Standard</div>
                    <div class="event-description">
                        Norgesmesterskapet i Standard format. Turneringens hovedarrangement med Norges beste spillere
                    </div>
                </a>

                <a href="#" class="event-card side-event" onclick="alert('Lenke til Modern Grand Challenge - implementer riktig URL')">
                    <div class="event-type">Grand Challenge</div>
                    <div class="event-title">Modern Grand Challenge 64</div>
                    <div class="event-description">
                        Eksklusiv Modern-turnering begrenset til 64 deltakere. Høyt konkurransenivå og attraktive premier
                    </div>
                </a>

                <a href="#" class="event-card side-event" onclick="alert('Lenke til Chaos Draft - implementer riktig URL')">
                    <div class="event-type">Fredag Event</div>
                    <div class="event-title">Chaos Draft</div>
                    <div class="event-description">
                        Avslappet og morsom draft-turnering på fredag kveld. Perfekt start på turneringshelgen
                    </div>
                </a>

                <a href="#" class="event-card qualifier" onclick="alert('Lenke til Last Chance - implementer riktig URL')">
                    <div class="event-type">Kvalifisering</div>
                    <div class="event-title">Last Chance Bye-Turnering</div>
                    <div class="event-description">
                        Siste sjanse til å kvalifisere seg til hovedturneringen. Standard format med verdifulle byes som premier
                    </div>
                </a>
            </div>
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <div class="footer-content">
                <div class="footer-brand">Norgesmesterskapet i Magic: The Gathering 2025</div>
                <div>Offisiell turnering for Norges Magic-miljø</div>
            </div>
        </div>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Smooth entrance animation
            const cards = document.querySelectorAll('.event-card');
            const observer = new IntersectionObserver((entries) => {
                entries.forEach((entry, index) => {
                    if (entry.isIntersecting) {
                        setTimeout(() => {
                            entry.target.style.opacity = '1';
                            entry.target.style.transform = 'translateY(0)';
                        }, index * 100);
                    }
                });
            }, { threshold: 0.1 });

            cards.forEach(card => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(30px)';
                card.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
                observer.observe(card);
            });

            // Hero card animation
            const heroCard = document.querySelector('.hero-card');
            heroCard.style.opacity = '0';
            heroCard.style.transform = 'translateY(20px)';
            
            setTimeout(() => {
                heroCard.style.transition = 'opacity 0.8s ease, transform 0.8s ease';
                heroCard.style.opacity = '1';
                heroCard.style.transform = 'translateY(0)';
            }, 300);
        });
    </script>
</body>
</html>